services:
  cs2-server:
    build: .
    restart: unless-stopped
    volumes:
      - /home/csgo/cs2:/home/steam/cs2 # Mapeamento direto para o diretório do servidor
    networks:
      - csgo-network

networks:
  csgo-network:
    driver: bridge
